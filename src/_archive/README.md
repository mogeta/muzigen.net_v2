# Archive

This directory stores implementation samples that are intentionally excluded from Astro routing.

- `posts-sample/`: legacy `/posts` sample pages moved from `src/pages/posts` to keep production focus on `/blog`.
